<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/util
        http://www.springframework.org/schema/util/spring-util-4.0.xsd">

	<util:map id="commonMap" map-class="java.util.HashMap">
		<entry key="domain" value="${domain}" />
		<entry key="memcache.url" value="${memcache.url}" />
		<entry key="component.appid" value="${component.appid}" />
		<entry key="component.appsecret" value="${component.appsecret}" />
		<entry key="component.token" value="${component.token}" />
		<entry key="component.encodingkey" value="${component.encodingkey}" />
		<entry key="component.oauth" value="${component.oauth}" />
		<entry key="callback.login.oauth" value="${callback.login.oauth}" />
		<entry key="mq." value="${mq.address}" />
	</util:map>
	
</beans>        